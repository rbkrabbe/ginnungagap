+++
id = "tk-3846"
title = "Server-side forwarding of leader-required requests to the shard leader"
kind = "epic"
status = "open"
size = "l"
priority = 2
blocked_by = []
tags = []
created = "2026-07-24T18:29:11+00:00"
spec_approved = true
review = "none"
touched = []
discovered_from = "tk-85c1"
spec_approved_at = "2026-07-28T14:51:03+00:00"
+++
## Context

The console KV browser reaches the cluster through a NodePort, so it lands on whichever node the service picks. Writes and linearizable reads that land on a follower fail with ForwardToLeader. The client cannot follow the leader hint itself: the addr openraft reports is an in-cluster DNS name (ggap-2.ggap-headless.ginnungagap.svc.cluster.local:17001) that a browser outside the cluster cannot resolve. Human decided the fix is server-side forwarding rather than a topology-aware proxy.

Shape: when ShardRouter/OpenRaftNode returns GgapError::NotLeader, KvServiceImpl proxies the request to the leader over the cluster network and returns the leader's response to the client, instead of surfacing the error.

Constraint discovered while sizing: the cluster-wide directory (registry.rs:52, cluster.proto GossipNode.cluster_addr) only carries each node's *cluster* gRPC address. KvService is currently registered on the client listener only (lib.rs:111, :142) — serve_cluster registers Raft/Admin/Gossip (lib.rs:180-187). So there is no existing path by which node A can issue a KV request to node B. Establishing one is the core of this task and the subject of Q1.

Also needs:
- Loop prevention — a forwarded request must never be forwarded again; a follower that receives a forwarded request and is not leader must fail, not bounce it onward.
- Leader identity, not just address: GgapError::NotLeader currently carries only Option<String>. Forwarding wants the leader's node_id so it can look the addr up in the registry rather than trusting the embedded one.
- Per-shard, not per-node: leadership is per shard (CLAUDE.md), so forwarding decisions are made per request against the routed shard.
- Connection reuse — do not dial a fresh channel per forwarded request; see GgapNetwork (network.rs:71) for the existing lazy-channel pattern.
- Observability: forwarded requests should be distinguishable in metrics/traces from locally-served ones.

Depends on tk-85c1, which makes NotLeader distinguishable from a generic consensus error on the linearizable-read path. (Done, merged as 933561a.)

## Scope widened 2026-07-25: AdminService too

Reported from the console: splitting a shard hits the same wall. Split is NOT the
tk-85c1 mapping bug — split.rs:149-157 already does the forward_to_leader() check
and admin_service.rs:203 deliberately surfaces NotLeader as a gRPC status rather
than burying it in the response body. Its mapping has always been correct. What it
shares with the KV browser is the second half: a correct leader hint the console
cannot act on.

The same applies to add_learner (admin_service.rs:143) and change_membership
(admin_service.rs:173) — pointed, since those are what you would use to join a
scaled-up node.

One asymmetry to carry into the spec: Q1's transport decision is moot for admin
ops. AdminService is already registered on the cluster listener (lib.rs:183) and
the console already reaches it over clusterTransport, so the node-to-node path
that Q1 exists to create for KV already exists for Admin. What the two surfaces
share is the mechanism, not the plumbing:

  resolve leader for shard -> dial peer from the gossip directory -> replay once
  -> never re-forward

Design it once as a shared component; both surfaces are applications of it. The
spec may propose shipping KV and Admin as separate PRs, and if it shows the whole
is too big for one task, convert to an epic then — with the design written once.

## Epic design (spec, 2026-07-25)

### Problem

A write or linearizable read that lands on a follower cannot be served. The
caller gets UNAVAILABLE plus a leader hint it cannot act on, because the hint is
an in-cluster DNS name and the caller is a browser reaching the cluster through
a NodePort that picks the node for it. Nobody can write to Ginnungagap through
the console without getting lucky about which node they land on.

### Approach

The receiving node resolves the leader for the routed shard and replays the
request there once, returning the leader's response verbatim. Three properties
hold the design together:

- **Identity, not address.** NotLeader carries the leader's node id; the address
  is looked up in the gossip directory at forward time rather than trusted from
  the error, which may be stale.
- **One hop, marked on the wire.** The forwarder sets `ggap-forwarded`; a
  receiver that sees it serves or fails but never forwards again.
- **Fail fast.** Leader unknown, unreachable, or moved mid-flight returns
  UNAVAILABLE with the hint. No server-side retry, so the caller keeps control
  of its own deadline.

Forwarded traffic goes to the leader's **client** port, learned from a
`client_addr` gossiped alongside `cluster_addr`.

### Rejected alternatives

- **KvService on the cluster listener** (the original Q1 answer, superseded by
  Q7). Reopened after finding that both 17000 and 17001 are already NodePorted
  (deploy/README.md:33-34), which voided the exposure premise the choice rested
  on. What remained was overload isolation, and this option spends it: forwarded
  client traffic would share a listener with Raft, so a client-load spike delays
  AppendEntries and can trigger spurious elections — precisely when the cluster
  is already unhappy. This is also why the two listeners stay separate at all;
  CockroachDB shipped a unified client/RPC port and moved away from it.
- **A hop path list allowing several hops with cycle detection.** A second hop
  only happens when the first target was not leader either, which is the
  leader-moved case Q2 already answered with fail-fast. It reopens that decision
  and spends the caller's deadline across hops with no budget tracking. Under a
  stable leader one hop always suffices; under an unstable one, client retry is
  cheaper and keeps the deadline where it belongs.
- **Listener-scoped forwarding capability as a second loop guard** (Q4). Would
  catch a forgotten marker, but adds a constructor flag whose two sites can be
  swapped, silently disabling forwarding while every no-loops test still passes.
  Trades a loud failure for a quiet one.
- **Server-side forwarding for AdminService** (Q8). Replaced by tk-f7b6, a
  bare-bones per-node proxy on the console's own nginx. The console's problem is
  that it cannot *address* a node, not that it cannot retry; nginx runs
  in-cluster and can resolve what the browser cannot. Real routing waits for a
  control plane.

### Vocabulary

- **Leader-required request** — a write or a linearizable read; anything that
  openraft answers with ForwardToLeader on a follower.
- **Forward** — one replay of a leader-required request from the receiving node
  to the shard leader, marked `ggap-forwarded`.
- **Directory** — ShardRegistry's node_id -> addresses map, gossiped and
  rebuildable, never persisted.

### Settled decisions

Q2 fail-fast, no server-side retry. Q3 Watch stays local. Q4 metadata marker is
the only loop guard. Q6 cluster-port exposure is documented, not enforced —
authz is the real gate and is separate future work. Q7 forward to the client
port via gossiped client_addr. Q8 Admin forwarding is out; tk-f7b6 replaces it.

### Children

- **tk-3cd2** (m, done) — NotLeader carries the leader's node id. Unblocked the rest.
- **tk-c507** (l) — client_addr through proto, registry and the gossip task.
  Filed `m`; resized by the scope hook when one signature change turned out to
  ripple through every `ShardRegistry::new` call site in the workspace.
- **tk-2cb2** (m, blocked by tk-c507) — the LeaderForwarder and KV forwarding.

tk-4f29 was the original single child covering both of the latter two; speccing
it split it (its Q3) and it is dropped.

Related, not a child: **tk-f7b6** (m) — console per-node proxy, the admin-side
stopgap.

## Acceptance

This epic is done when both children are, and:

- [ ] A write and a linearizable read sent to a follower both succeed end to end
      from the console KV browser against a real multi-node cluster.
- [ ] No request is ever forwarded twice.
- [ ] Raft traffic and forwarded client traffic remain on separate listeners.

### Q1 [answered 2026-07-24T18:32:00+00:00] How does a node issue a KV request to the leader? The cluster-wide directory only knows cluster addrs, and KvService is only served on the client listener.
- a) Serve KvService on the cluster listener too — reuses the existing directory and the proto as-is; widens what the cluster port exposes, so the cluster port must stay non-public.
- b) Add an internal Forward RPC to cluster.proto — explicit forwarding envelope, easy to mark hop count and reject double-forwards; costs a new proto surface plus encode/decode of every KV request type.
- c) Gossip a client_addr alongside cluster_addr and dial the leader's client port — keeps the client API on one listener; adds a field to GossipNode and makes every node's client port reachable node-to-node.
> Serve KvService on the cluster listener too — register KvServiceServer in serve_cluster/serve_cluster_with_listener alongside Raft/Admin/Gossip. Reuses the existing gossip directory (cluster_addr) and the proto unchanged. Implication to honour: the cluster port now exposes the client API, so it must stay off the NodePort / off any public route.

### Q2 [answered 2026-07-24T18:32:00+00:00] What happens when the leader moves or is unknown while a request is being forwarded?
- a) Fail fast — return UNAVAILABLE with the leader hint and let the caller retry; simplest, and the console can retry cheaply.
- b) Bounded retry inside the server — re-resolve the leader and retry N times before failing; hides brief elections from the client, costs a retry budget and a deadline to respect.
> Fail fast — return UNAVAILABLE with the leader hint when the leader is unknown or moves mid-forward; the caller retries. No server-side retry loop, no hidden latency, no deadline budget to manage.

### Q3 [answered 2026-07-24T18:32:00+00:00] Does Watch forward too?
- a) Writes and linearizable reads only — Watch stays local and out of scope; smallest surface, streaming forwarding is its own problem.
- b) Watch forwards as well — consistent behaviour across the whole KV API, but proxying a long-lived server stream needs its own lifecycle and cancellation handling.
> Writes and linearizable reads only. Watch stays local and out of scope; proxying a long-lived server stream is a separate task if it turns out to be needed.

### Q4 [answered 2026-07-25T09:58:19+00:00] How does a node tell that an incoming request is already a forward, so it never forwards again?
- a) Listener-scoped capability — the KvServiceImpl registered on the cluster listener is constructed with forwarding disabled, the client-listener one with it enabled; looping is structurally impossible and nothing goes on the wire, but AdminService lives only on the cluster listener so it cannot use this and needs a second mechanism
- b) gRPC metadata marker (ggap-forwarded) — one mechanism for both KV and Admin, set by the forwarder and refused-if-present by the receiver; an external client could set the header itself, whose only effect is getting NotLeader instead of a forward
- c) Both — metadata marker as the wire contract, listener scope as defence in depth for KV
> Metadata marker only — one mechanism across KV and Admin; listener-scoped flags add a swap-the-flags failure that silently disables forwarding, and a hop path list reopens the Q2 fail-fast decision

### Q5 [answered 2026-07-25T09:58:19+00:00] Ship as one task or split into an epic?
- a) Epic with three children — (A) NotLeader carries leader_id, (B) shared forwarder + KV forwarding + KvService on cluster listener, (C) Admin forwarding; each independently reviewable and A unblocks the rest
- b) One task as specced — single PR, no cross-task coordination, but seven files and two gRPC surfaces in one adversarial review
> Epic with three children — A NotLeader carries leader_id, B shared forwarder plus KV forwarding, C Admin forwarding; A unblocks B and C

### Q6 [answered 2026-07-25T09:58:19+00:00] Q1 requires the cluster port stay off any public route once it serves the KV API. Is enforcing that in scope?
- a) Document only — a comment in serve_cluster and a note in CLAUDE.md; deployment already keeps the cluster port off the NodePort
- b) Enforce in code — startup refuses to serve KV on the cluster listener unless config explicitly acknowledges it, so a misconfigured deploy fails loudly rather than silently exposing the KV API
> Document only — port reachability is not the real gate; proper authz will be, and that is separate future work. Comment in serve_cluster plus a CLAUDE.md note is enough.

### Q7 [answered 2026-07-25T10:11:59+00:00] SUPERSEDES Q1 — reopened after finding both ports are already NodePorted (deploy/README.md:33-34), which voids the exposure premise Q1 was decided on. Which transport?
- a) (c) Gossip client_addr and dial the leader's client port — preserves Raft/client listener isolation; costs one GossipNode field plus directory plumbing
- b) (a) Serve KvService on the cluster listener as originally answered — no proto change, but forwarded client traffic shares a listener with Raft
> (c) gossip client_addr — the exposure argument that favoured (a) rested on a wrong premise; both 17000 and 17001 are already NodePorted, so (a) adds no meaningful exposure but does erode Raft/client overload isolation, which (c) preserves

### Q8 [answered 2026-07-25T10:11:59+00:00] SUPERSEDES Q5 — does Admin forwarding stay a child of this epic?
- a) No — drop child C; ship A and B only, and file a bare-bones proxy in the console service as the stopgap for admin commands until a real control plane exists
- b) Yes — keep C as originally answered, forwarding admin ops server-side
> No — drop child C. A bare-bones proxy in the console service is the stopgap for admin commands until a full control plane can serve them properly.
