+++
id = "tk-10d8"
title = "Invert the addresses-in-membership rule in CLAUDE.md and docs"
kind = "task"
status = "open"
size = "s"
priority = 2
blocked_by = ["tk-abf8", "tk-00c0"]
tags = []
created = "2026-08-15T10:19:15+00:00"
spec_approved = false
review = "none"
touched = []
parent = "tk-ef8d"
+++
## Context

CLAUDE.md carries a hard constraint added by tk-1a8d that this epic makes
exactly wrong: "Per-node addresses live in Raft membership, never in gossip."
Invert it, do not soften it — the replacement rule is that consensus carries
identity and the directory resolves it, and the reason is multi-raft.

`docs/ggap-consensus.md` and `docs/ggap-types.md` were rewritten by tk-1a8d to
describe the derived directory. Both need the opposite. State the end state; do
not narrate the reversal.

## Acceptance

- [ ] No doc describes the directory as derived from membership.
- [ ] CLAUDE.md carries the inverted constraint and a Current State that matches.
- [ ] Markdown-only unless a source doc comment is stale, which is likely — check
      `NodeAddrs` and `GgapNode` before assuming the checklist can be skipped.

