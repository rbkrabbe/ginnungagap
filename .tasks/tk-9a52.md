+++
id = "tk-9a52"
title = "Regenerate the console's TS client for the RemoveNode RPC"
kind = "task"
status = "open"
size = "s"
priority = 3
blocked_by = []
tags = []
created = "2026-08-25T19:41:56+00:00"
spec_approved = false
review = "none"
touched = []
discovered_from = "tk-c47e"
+++
## Context

tk-c47e added `AdminService.RemoveNode` and a `removed` field on `GossipNode`.
`console/src/gen/ginnungagap/v1/*` is generated from the proto and checked in,
but neither `buf` nor `console/node_modules` is present on this machine, so it
was left at the previous schema.

Nothing is broken: no console screen calls `RemoveNode` or reads `GossipNode`.
The generated client is simply behind the proto.

tk-8d80 regenerates the same directory for its own reasons; if it lands first,
this is already done and the task closes.

## Acceptance

- [ ] `console/src/gen` matches `proto/ginnungagap/v1/*.proto`.
- [ ] `npm run build` in `console/` succeeds.
