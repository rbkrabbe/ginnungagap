+++
id = "tk-c271"
title = "Skip build/lint/test when a change touches no code"
kind = "task"
status = "in_progress"
size = "s"
priority = 2
blocked_by = []
tags = []
created = "2026-07-25T09:21:06+00:00"
spec_approved = false
review = "none"
touched = [".github/workflows/ci.yml", "CLAUDE.md"]
+++
## Context

_TODO_

## Acceptance

- [ ] TODO

### Q1 [answered 2026-07-25T09:23:37+00:00] How should CI skip the Rust steps for docs/task-only changes?
- a) Gate the steps, always report — job runs, a first step diffs the changed paths and each cargo step gets an if:; ci always reports success so a required status check is still satisfied; costs a few seconds of runner startup per PR
- b) paths-ignore on the workflow trigger — simplest YAML, zero runner time, but the ci check never reports at all on a skipped PR, so the moment branch protection is enabled per the header comment in ci.yml, docs-only PRs hang unmergeable on a pending required check
> Gate the steps, always report — ci must still report success on docs-only PRs so a required status check stays satisfiable when branch protection is enabled
