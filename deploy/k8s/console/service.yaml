apiVersion: v1
kind: Service
metadata:
  name: console
  namespace: ginnungagap
  labels:
    app.kubernetes.io/name: console
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: console
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30880
